sudo: required

language: node_js
node_js:
        - "stable"

cache:
        directories:
                - node_modules

script:
        - cd server && npm install
        - cd ../client && npm install
        - npm test
        - npm run build

