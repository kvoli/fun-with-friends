const getUsername = (state) => state.auth.username;
const getFirstname = (state) => state.auth.firstname;
const getLastname = (state) => state.auth.lastname;
const getEmail = (state) => state.auth.email;
const getToken = (state) => state.auth.token;
